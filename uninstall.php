<?php

if (!defined('WP_UNINSTALL_PLUGIN')) {
    echo "Sorry, this script can only be executed from within Wordpress";
    die;
}

//delete_option('option name');

//global $wpdb;
//$wpdb->query("DROP TABLE IF EXISTS {$wpdb->prefix}mytable");
